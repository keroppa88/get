name: Sync pricedata to chart0

on:
  push:
    branches: [ main ]
    paths:
      - 'data/pricedata/TOPIX (東証株価指数).csv'
      - 'data/pricedata/日経平均ボラティリティー・インデックス.csv'
      - 'data/pricedata/日経平均株価.csv'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout get
        uses: actions/checkout@v4

      - name: Clone chart0
        run: |
          git clone https://x-access-token:${{ secrets.SYNC_TOKEN }}@github.com/keroppa88/chart0.git chart0

      - name: Copy CSV files
        run: |
          cp -f "data/pricedata/TOPIX (東証株価指数).csv" chart0/data/
          cp -f "data/pricedata/日経平均ボラティリティー・インデックス.csv" chart0/data/
          cp -f "data/pricedata/日経平均株価.csv" chart0/data/

      - name: Commit & push if changed
        run: |
          cd chart0
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git status --porcelain | grep -q .; then
            git add data/
            git commit -m "sync: pricedata from get [skip ci]"
            git push
          else
            echo "No changes; skip."
          fi
